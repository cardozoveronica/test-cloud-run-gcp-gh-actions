name: Run unit tests

on:
  workflow_call:

env:
  NODE_VERSION: 22

jobs:
# El nombre del job
  do_nothing:
    # El sistema operativo donde se ejecutará (puedes usar 'ubuntu-latest', 'windows-latest', etc.)
    runs-on: ubuntu-latest
    
    # Los pasos (steps) que componen el job
    steps:
      # Un paso simple para mostrar un mensaje
      - name: Just print a message
        run: |
          echo "Este workflow de prueba se ha ejecutado."
          echo "Razon: ${{ github.event.inputs.reason }}"
          echo "Hora: $(date)"
